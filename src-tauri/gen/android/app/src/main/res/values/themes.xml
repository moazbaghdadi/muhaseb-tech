<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.muhaseb_tech" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status-bar icon color is forced from MainActivity.enableEdgeToEdge — see
             SystemBarStyle.light(...) there. Setting it here would be overridden. -->
    </style>
</resources>
